{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
      "builder": "NIXPACKS"
  },
  "deploy": {
      "startCommand": "pip install psycopg2 && alembic upgrade head && gunicorn main:app",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
  }
}